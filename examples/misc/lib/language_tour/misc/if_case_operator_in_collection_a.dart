void main() {
  // #docregion code_sample
  var a = 'apple';
  var b = 'orange';
  var items = [
    0,
    if (a case 'apple') 1,
    if (b case 'pear') 2,
    3,
    4,
  ]; // [0, 1, 3, 4]
  // #enddocregion code_sample

  print(items);
}
