void main() {
  // #docregion code_sample
  var a = 'one';
  var b = '';
  var items = [
    0,
    if (a case 'one') 1,
    if (b case 'two') 2,
    3,
    4,
  ]; // [0, 1, 3, 4]
  // #enddocregion code_sample

  print(items);
}
