void main() {
  /* HACK: This code is fails the type checker, so I don't want to actually run it.
    // #docregion
    var name = 'Bob';
    if (name) {
      // Prints in JavaScript, not in Dart.
      print('You have a name!');
    }
    // #enddocregion
  */
}
