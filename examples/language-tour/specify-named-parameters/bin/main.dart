void main() {
  // BEGIN(specify_named_parameters)
  /// Sets the [bold] and [hidden] flags to the values
  /// you specify.
  void enableFlags({bool bold, bool hidden}) {
    //...
  }
  // END(specify_named_parameters)
}
