name: sdk_builds
description: Utilities for accessing builds of the Dart SDK.

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  googleapis: ^9.0.0
  http: ^0.13.1
  path: ^1.8.0
  pub_semver: ^2.0.0

dev_dependencies:
  test: ^1.19.2
  lints: ^2.0.0
