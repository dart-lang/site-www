name: get_dart
publish_to: none

environment:
  sdk: ^3.5.3

workspace:
  - archive
  - dart_sdk_archive
  - sdk_builds

dev_dependencies:
  analysis_defaults:
    path: ../../site-shared/pkgs/analysis_defaults
